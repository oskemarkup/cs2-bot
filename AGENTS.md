# Project Instructions

Before running any Node.js, npm, yarn, pnpm, or related project command in this repository, run:

```bash
source ~/.nvm/nvm.sh && nvm use
```

This project uses the Node.js version from `.nvmrc`.
